# Gradius Frontend Tasks

- [ ] Initialize React Flow package and basic map layout structure.
- [ ] Build global authentication state context or NextAuth implementation.
- [ ] Create Login and Register page interfaces.
- [ ] Build layout wrapper containing the Progress Summary Header (Total, Pending, Approved metrics).
- [ ] Implement CSV/JSON file upload component for submitting career path data.
- [ ] Create Custom Subject Node Component for the React Flow map with distinct styles based on status (Pending, Regular, Approved).
- [ ] Wire map component to fetch map data and user progress from the backend.
- [ ] Create click handlers in the Subject Node to trigger API status updates via `PATCH` and update local state incrementally.
- [ ] Apply rich aesthetic styles and micro-animations to map and cards.
